fastapi>=0.115.0
uvicorn[standard]>=0.32.0
rdkit>=2024.3.0
numpy>=1.26.0
scipy>=1.13.0
pydantic>=2.9.0
# httpx is used for async HTTP calls to ChEMBL, UniProt, Open Targets, ClinicalTrials.gov
httpx>=0.27.0
# gql is available if needed for GraphQL introspection; Open Targets uses plain httpx POST
gql[httpx]>=3.5.0
